# dotfile
dotfile(config for some of the linux software)
# usage
put the files into your directory like ~/.config or these the distribution that use it for putting the config files
remember to install the i3 group, rofi, playerctl as well as dmenu and compton before you use it
> also
the softwares like wine and clementine can be changed into what you whant and
> config the fixed container for workspace
install xprop for the system, run the program you'd like to config and run a terminal at the same workspace. Then run xprop and when the mouse becomes a cross put it on the specific program and click. Copy the string like Class="" as you can see in the config file
# list of what I installed can be find after every exec command in the config file
todo1
     todo2
